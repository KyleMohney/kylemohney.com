<!DOCTYPE html>

<html lang="en">
<head><style>
        /* Performance optimizations */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            animation: none !important;
            transition: none !important;
        }

        body {
            font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
            line-height: 1.6;
            color: #2d3748;
            background: #f8faf9;
            min-height: 100vh;
            overflow-x: hidden;
            position: relative;
        }

        /* Enhanced background glow */
        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: 
                radial-gradient(circle at 15% 25%, rgba(152, 229, 142, 0.08) 0%, transparent 50%),
                radial-gradient(circle at 85% 75%, rgba(0, 107, 64, 0.06) 0%, transparent 45%),
                radial-gradient(circle at 50% 50%, rgba(152, 229, 142, 0.03) 0%, transparent 60%);
            pointer-events: none;
            z-index: -2;
        }

        /* Enhanced Circuit Animation with dynamic flowing lines like index.html */
        body::after {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-image: 
                linear-gradient(90deg, rgba(152, 229, 142, 0.1) 2px, transparent 2px),
                linear-gradient(0deg, rgba(152, 229, 142, 0.1) 2px, transparent 2px),
                linear-gradient(45deg, rgba(152, 229, 142, 0.06) 1px, transparent 1px),
                linear-gradient(-45deg, rgba(152, 229, 142, 0.06) 1px, transparent 1px);
            background-size: 60px 60px, 60px 60px, 30px 30px, 30px 30px;
            pointer-events: none;
            z-index: -1;
            opacity: 0.3;
        }

        /* Enhanced Floating Data Nodes - More particles for high-tech look */
        .floating-nodes {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: -1;
        }

        .data-node {
            position: absolute;
            width: 6px;
            height: 6px;
            background: radial-gradient(circle, rgba(152, 229, 142, 0.9) 0%, rgba(0, 107, 64, 0.6) 100%);
            border-radius: 50%;
            box-shadow: 
                0 0 20px rgba(152, 229, 142, 0.4),
                0 0 40px rgba(152, 229, 142, 0.2);
            opacity: 0.3;
        }

        /* Add 12 data nodes for more high-tech appearance */
        .data-node:nth-child(1) { top: 20%; left: 10%; animation-delay: 0s; }
        .data-node:nth-child(2) { top: 60%; left: 85%; animation-delay: 2s; }
        .data-node:nth-child(3) { top: 80%; left: 25%; animation-delay: 4s; }
        .data-node:nth-child(4) { top: 30%; left: 70%; animation-delay: 6s; }
        .data-node:nth-child(5) { top: 15%; left: 45%; animation-delay: 8s; }
        .data-node:nth-child(6) { top: 75%; left: 60%; animation-delay: 10s; }
        .data-node:nth-child(7) { top: 40%; left: 15%; animation-delay: 12s; }
        .data-node:nth-child(8) { top: 65%; left: 90%; animation-delay: 14s; }
        .data-node:nth-child(9) { top: 5%; left: 75%; animation-delay: 1s; }
        .data-node:nth-child(10) { top: 90%; left: 35%; animation-delay: 3s; }
        .data-node:nth-child(11) { top: 45%; left: 95%; animation-delay: 5s; }
        .data-node:nth-child(12) { top: 85%; left: 5%; animation-delay: 7s; }

        @keyframes nodeFloat {
            0%, 100% { 
                transform: translate3d(0, 0, 0) scale(1); 
                opacity: 0.6;
                box-shadow: 0 0 20px rgba(152, 229, 142, 0.4);
            }
            25% { 
                transform: translate3d(30px, -40px, 0) scale(1.3); 
                opacity: 1;
                box-shadow: 0 0 30px rgba(152, 229, 142, 0.6);
            }
            50% { 
                transform: translate3d(-20px, 30px, 0) scale(0.8); 
                opacity: 0.8;
                box-shadow: 0 0 25px rgba(152, 229, 142, 0.5);
            }
            75% { 
                transform: translate3d(40px, 15px, 0) scale(1.2); 
                opacity: 0.9;
                box-shadow: 0 0 35px rgba(152, 229, 142, 0.7);
            }
        }

        /* Dynamic Signal Lines System - matching index.html exactly */
        .signal-lines {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: -1;
            opacity: 0.6;
        }

        .signal-line {
            position: absolute;
            height: 3px;
            background: linear-gradient(90deg, 
                transparent 0%, 
                rgba(152, 229, 142, 0.8) 20%, 
                #98e58e 50%, 
                rgba(152, 229, 142, 0.8) 80%, 
                transparent 100%);
            border-radius: 2px;
            animation: signalFlow 4s linear infinite;
            box-shadow: 0 0 10px rgba(152, 229, 142, 0.5);
        }

        .signal-line:nth-child(1) { 
            top: 20%; left: 0; width: 100%; 
            animation-delay: 0s; 
            transform: rotate(0deg);
        }
        .signal-line:nth-child(2) { 
            top: 50%; left: 0; width: 100%; 
            animation-delay: 1.5s; 
            transform: rotate(0deg);
        }
        .signal-line:nth-child(3) { 
            top: 75%; left: 0; width: 100%; 
            animation-delay: 3s; 
            transform: rotate(0deg);
        }

        /* Vertical signal lines */
        .signal-vertical {
            position: absolute;
            width: 3px;
            background: linear-gradient(0deg, 
                transparent 0%, 
                rgba(152, 229, 142, 0.6) 20%, 
                #98e58e 50%, 
                rgba(152, 229, 142, 0.6) 80%, 
                transparent 100%);
            border-radius: 2px;
            animation: signalFlowVertical 5s linear infinite;
            box-shadow: 0 0 8px rgba(152, 229, 142, 0.4);
        }

        .signal-vertical:nth-child(4) { 
            left: 25%; top: 0; height: 100%; 
            animation-delay: 0.5s; 
        }
        .signal-vertical:nth-child(5) { 
            left: 60%; top: 0; height: 100%; 
            animation-delay: 2s; 
        }
        .signal-vertical:nth-child(6) { 
            left: 85%; top: 0; height: 100%; 
            animation-delay: 3.5s; 
        }

        @keyframes signalFlow {
            0% { transform: translateX(-100%) scaleX(0); opacity: 0; }
            20% { transform: translateX(-50%) scaleX(1); opacity: 1; }
            80% { transform: translateX(50%) scaleX(1); opacity: 1; }
            100% { transform: translateX(100%) scaleX(0); opacity: 0; }
        }

        @keyframes signalFlowVertical {
            0% { transform: translateY(-100%) scaleY(0); opacity: 0; }
            20% { transform: translateY(-50%) scaleY(1); opacity: 1; }
            80% { transform: translateY(50%) scaleY(1); opacity: 1; }
            100% { transform: translateY(100%) scaleY(0); opacity: 0; }
        }
        .tech-connectors {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            pointer-events: none;
            z-index: -1;
            opacity: 0.3;
        }

        .circuit-connector {
            position: absolute;
            background: linear-gradient(90deg, transparent, rgba(152, 229, 142, 0.6), transparent);
            height: 2px;
            animation: pulseData 4s ease-in-out infinite;
        }

        .circuit-connector:nth-child(1) { top: 25%; left: 0; width: 100%; animation-delay: 0s; }
        .circuit-connector:nth-child(2) { top: 50%; left: 0; width: 100%; animation-delay: 1s; }
        .circuit-connector:nth-child(3) { top: 75%; left: 0; width: 100%; animation-delay: 2s; }

        .circuit-connector.vertical {
            width: 2px;
            height: 100%;
            background: linear-gradient(0deg, transparent, rgba(152, 229, 142, 0.6), transparent);
        }

        .circuit-connector.vertical:nth-child(4) { left: 20%; top: 0; animation-delay: 0.5s; }
        .circuit-connector.vertical:nth-child(5) { left: 50%; top: 0; animation-delay: 1.5s; }
        .circuit-connector.vertical:nth-child(6) { left: 80%; top: 0; animation-delay: 2.5s; }

        @keyframes pulseData {
            0%, 100% { opacity: 0.2; transform: scaleX(1); }
            50% { opacity: 0.8; transform: scaleX(1.05); }
        }

        /* Icon Rotation on Hover */
        .section-icon,
        .sidebar-icon {
        }

        .section-header:hover .section-icon,
        .sidebar-title:hover .sidebar-icon {
            transform: rotate(5deg) scale(1.1);
        }

        /* Enhanced Sprout Highlight */
        .sprout-highlight {
            background: linear-gradient(135deg, #006b40 0%, #98e58e 100%);
            color: white;
            padding: 32px;
            border-radius: 16px;
            margin: 24px 0;
            box-shadow: 
                0 12px 40px rgba(0, 107, 64, 0.2),
                0 0 0 1px rgba(255, 255, 255, 0.1);
            position: relative;
            overflow: hidden;
        }

        .sprout-highlight::before {
            content: '';
            position: absolute;
            top: -50%;
            right: -50%;
            width: 200%;
            height: 200%;
            background: linear-gradient(45deg, transparent 30%, rgba(255, 255, 255, 0.1) 50%, transparent 70%);
            animation: shimmer 4s ease-in-out infinite;
        }

        @keyframes shimmer {
            0%, 100% { transform: translateX(-100%) translateY(-100%) rotate(45deg); }
            50% { transform: translateX(100%) translateY(100%) rotate(45deg); }
        }

        *:focus {
            outline: 2px solid #98e58e;
            outline-offset: 2px;
        }

        img {
            max-width: 100%;
            height: auto;
        }

        /* Header Navigation */
        .header {
            padding: 24px 0;
            position: sticky;
            top: 0;
            background: rgba(248, 250, 249, 0.95);
            backdrop-filter: blur(10px);
            z-index: 1000;
            border-bottom: 1px solid rgba(152, 229, 142, 0.1);
            box-shadow: 0 2px 20px rgba(0, 107, 64, 0.05);
        }

        .nav-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .profile-logo {
            width: 52px;
            height: 52px;
            border-radius: 50%;
            border: 2px solid #98e58e;
            object-fit: cover;
        }

        .logo-text {
            font-size: 1.5em;
            font-weight: 700;
            color: #2d3748;
        }

        .nav-menu {
            display: flex;
            list-style: none;
            gap: 40px;
        }

        .nav-menu a {
            color: #2d3748;
            text-decoration: none;
            font-weight: 600;
            font-size: 1.1em;
            padding: 12px 20px;
            border-radius: 8px;
            position: relative;
        }

        .nav-menu a:hover {
            color: #006b40;
            background: rgba(152, 229, 142, 0.1);
            transform: translateY(-2px);
        }

        .nav-menu a.current {
            color: #006b40;
            background: rgba(152, 229, 142, 0.15);
            box-shadow: 0 4px 12px rgba(152, 229, 142, 0.2);
        }

        /* Resume Header */
        .resume-header {
            background: linear-gradient(135deg, #006b40 0%, #98e58e 100%);
            padding: 80px 0;
            position: relative;
            margin-bottom: 60px;
        }

        .resume-header::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: 
                radial-gradient(circle at 20% 30%, rgba(255, 255, 255, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 80% 70%, rgba(255, 255, 255, 0.05) 0%, transparent 40%);
            z-index: 0;
        }

        .header-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
            position: relative;
            z-index: 1;
            display: grid;
            grid-template-columns: auto 1fr auto;
            gap: 40px;
            align-items: center;
        }

        .sprout-badge-section {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 16px;
        }

        .sprout-logo-large {
            width: 90px;
            height: 90px;
            background: white;
            border-radius: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 12px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            position: relative;
        }

        .sprout-logo-large img {
            width: 100%;
            height: 100%;
            object-fit: contain;
        }

        .sprout-specialist-badge {
            background: rgba(255, 255, 255, 0.2);
            color: white;
            padding: 12px 20px;
            border-radius: 25px;
            font-size: 0.8em;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.8px;
            text-align: center;
            box-shadow: 0 8px 24px rgba(0, 0, 0, 0.1);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.3);
            line-height: 1.3;
        }

        .name-title-section {
            text-align: center;
        }

        .resume-name {
            font-size: 4.5em;
            font-weight: 800;
            margin-bottom: 16px;
            color: white;
            line-height: 1.1;
            text-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
        }

        .resume-title {
            font-size: 1.8em;
            color: rgba(255, 255, 255, 0.9);
            margin-bottom: 32px;
            font-weight: 400;
        }

        .contact-info {
            display: flex;
            justify-content: center;
            gap: 24px;
            flex-wrap: wrap;
        }

        .contact-item {
            display: flex;
            align-items: center;
            gap: 8px;
            background: rgba(255, 255, 255, 0.15);
            color: white;
            padding: 12px 20px;
            border-radius: 12px;
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            font-weight: 500;
        }

        .profile-image-large {
            width: 130px;
            height: 130px;
            border-radius: 50%;
            border: 4px solid white;
            object-fit: cover;
            object-position: center;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
        }

        /* Main Content */
        .resume-content {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px 40px;
            display: grid;
            grid-template-columns: 2fr 1fr;
            gap: 40px;
        }

        .main-content {
            background: rgba(255, 255, 255, 0.45);
            backdrop-filter: blur(12px);
            border-radius: 24px;
            padding: 48px;
            box-shadow: 0 20px 60px rgba(0, 107, 64, 0.08);
            border: 1px solid rgba(152, 229, 142, 0.3);
            position: relative;
            overflow: hidden;
        }

        .main-content::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, #006b40, #98e58e);
        }

        .sidebar {
            display: flex;
            flex-direction: column;
            gap: 24px;
        }

        .section-header {
            display: flex;
            align-items: center;
            gap: 20px;
            margin-bottom: 40px;
            padding-bottom: 20px;
            border-bottom: 2px solid rgba(152, 229, 142, 0.2);
        }

        .section-icon {
            width: 48px;
            height: 48px;
            background: linear-gradient(135deg, #006b40, #98e58e);
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 12px;
            box-shadow: 0 8px 24px rgba(0, 107, 64, 0.2);
        }

        .section-icon svg {
            width: 24px;
            height: 24px;
            fill: white;
        }

        .section-title {
            font-size: 2.4em;
            font-weight: 700;
            color: #2d3748;
        }

        /* Value Proposition */
        .value-proposition {
            background: linear-gradient(135deg, rgba(248, 251, 248, 0.5) 0%, rgba(235, 249, 235, 0.5) 100%);
            backdrop-filter: blur(10px);
            color: #2d3748;
            padding: 40px;
            border-radius: 20px;
            margin-bottom: 48px;
            font-size: 1.15em;
            line-height: 1.8;
            border: 2px solid rgba(152, 229, 142, 0.4);
            box-shadow: 0 8px 32px rgba(0, 107, 64, 0.05);
            position: relative;
        }

        .value-proposition::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, #006b40, #98e58e);
            border-radius: 20px 20px 0 0;
        }

        .value-proposition strong {
            color: #006b40;
            font-weight: 700;
        }

        /* Experience */
        .experience-timeline {
            position: relative;
            padding-left: 40px;
        }

        .experience-timeline::before {
            content: '';
            position: absolute;
            left: 16px;
            top: 0;
            bottom: 0;
            width: 4px;
            background: linear-gradient(180deg, #98e58e, #006b40);
            border-radius: 2px;
        }

        /* Hover-only effects for cards - no distracting automatic animations */
        .experience-card,
        .sidebar-card {

        }

        .experience-card {
            position: relative;
            background: white;
            border-radius: 20px;
            padding: 40px;
            margin-bottom: 32px;
            border: 2px solid rgba(152, 229, 142, 0.1);
            box-shadow: 0 8px 32px rgba(0, 107, 64, 0.05);
        }

        .experience-card::before {
            content: '';
            position: absolute;
            left: -52px;
            top: 32px;
            width: 24px;
            height: 24px;
            background: #98e58e;
            border: 4px solid white;
            border-radius: 50%;
            box-shadow: 0 0 0 4px #98e58e;
        }

        .experience-card:hover {
            transform: translateY(-8px) scale(1.02);
            border-color: #98e58e;
            box-shadow: 
                0 20px 60px rgba(0, 107, 64, 0.15),
                0 0 40px rgba(152, 229, 142, 0.4),
                inset 0 0 20px rgba(152, 229, 142, 0.05);
            background: linear-gradient(135deg, #ffffff 0%, #f8fbf8 100%);
        }

        .company-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }

        .company-name {
            font-size: 1.8em;
            font-weight: 700;
            color: #2d3748;
            display: flex;
            align-items: center;
            gap: 20px;
        }

        .sprout-indicator {
            background: linear-gradient(135deg, #006b40, #98e58e);
            color: white;
            padding: 6px 16px;
            border-radius: 20px;
            font-size: 0.65em;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.4px;
            box-shadow: 0 4px 12px rgba(0, 107, 64, 0.3);
            white-space: nowrap;
            flex-shrink: 0;
        }

        .location {
            color: #006b40;
            font-weight: 600;
            font-size: 1.1em;
        }

        .position {
            font-size: 1.4em;
            font-weight: 600;
            color: #006b40;
            margin-bottom: 8px;
        }

        .duration {
            color: #718096;
            font-size: 1em;
            margin-bottom: 28px;
            font-weight: 500;
        }

        .sprout-highlight h4 {
            margin-bottom: 16px;
            font-size: 1.3em;
            display: flex;
            align-items: center;
            gap: 12px;
        }

        /* Fixed scrolling metrics - better centering and smooth animation */
        .metrics-grid.scrolling {
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
            position: relative;
            height: 140px;
            border-radius: 16px;
            background: linear-gradient(135deg, #f8fbf8 0%, #ebf9eb 100%);
            border: 2px solid #98e58e;
            margin: 32px 0;
        }

        .metrics-track {
            display: flex;
            flex-direction: column;
            animation: scrollMetricsVertical 16s ease-in-out infinite;
            height: max-content;
            width: 100%;
        }

        .metrics-track .metric-card {
            min-height: 140px;
            width: 100%;
            margin: 0;
            background: transparent;
            border: none;
            box-shadow: none;
            padding: 20px 32px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
        }

        .metrics-track .metric-card .metric-number {
            font-size: 2.2em;
            margin-bottom: 8px;
            font-weight: 700;
            color: #006b40;
        }

        .metrics-track .metric-card .metric-label {
            font-size: 1em;
            line-height: 1.3;
            color: #2d3748;
            font-weight: 600;
        }

        @keyframes scrollMetricsVertical {
            0%, 20% { transform: translateY(0); }
            25%, 45% { transform: translateY(-140px); }
            50%, 70% { transform: translateY(-280px); }
            75%, 95% { transform: translateY(-420px); }
            100% { transform: translateY(0); }
        }

        .metrics-track:hover {
            animation-play-state: paused;
        }

        .metrics-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 20px;
            margin: 32px 0;
        }

        .metric-card {
            background: linear-gradient(135deg, rgba(248, 251, 248, 0.4) 0%, rgba(235, 249, 235, 0.4) 100%);
            backdrop-filter: blur(8px);
            border: 2px solid rgba(152, 229, 142, 0.6);
            padding: 28px;
            border-radius: 16px;
            text-align: center;
            box-shadow: 0 8px 24px rgba(0, 107, 64, 0.1);
            transition: all 0.3s ease;
        }

        .metric-card:hover {
            transform: translateY(-4px) scale(1.02);
            background: linear-gradient(135deg, rgba(240, 249, 240, 0.6) 0%, rgba(232, 245, 232, 0.6) 100%);
            backdrop-filter: blur(12px);
            box-shadow: 
                0 15px 40px rgba(0, 107, 64, 0.2),
                0 0 25px rgba(152, 229, 142, 0.4);
        }

        /* Counting Animation for Metrics */
        .metric-number {
            font-size: 2em;
            font-weight: 700;
            color: #006b40;
            margin-bottom: 8px;
            display: block;
            position: relative;
            overflow: hidden;
        }

        .metric-number.animate {
            animation: countUp 2s ease-out forwards;
        }

        @keyframes countUp {
            from { transform: translateY(100%); opacity: 0; }
            to { transform: translateY(0); opacity: 1; }
        }

        .metric-label {
            font-size: 0.95em;
            color: #2d3748;
            font-weight: 600;
        }

        .achievement-list {
            list-style: none;
            padding: 0;
        }

        .achievement-list li {
            position: relative;
            padding-left: 32px;
            margin-bottom: 20px;
            line-height: 1.7;
            color: #4a5568;
            font-size: 1.05em;
        }

        .achievement-list li::before {
            content: '';
            position: absolute;
            left: 0;
            top: 12px;
            width: 8px;
            height: 8px;
            background: linear-gradient(135deg, #006b40, #98e58e);
            border-radius: 50%;
            box-shadow: 0 2px 8px rgba(0, 107, 64, 0.3);
        }

        .f-pattern-hook {
            font-weight: 700;
            color: #006b40;
        }

        .highlight-metric {
            background: linear-gradient(135deg, #006b40, #98e58e);
            color: white;
            padding: 4px 12px;
            border-radius: 8px;
            font-weight: 700;
            font-size: 0.9em;
            box-shadow: 0 2px 8px rgba(0, 107, 64, 0.2);
            white-space: nowrap;
            display: inline-block;
        }

        /* Sidebar */
        .sidebar-card {
            background: white;
            border-radius: 20px;
            padding: 32px;
            border: 2px solid rgba(152, 229, 142, 0.1);
            transition: all 0.3s ease;
            box-shadow: 0 8px 32px rgba(0, 107, 64, 0.05);
            position: relative;
            overflow: hidden;
        }

        .sidebar-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 3px;
            background: linear-gradient(90deg, #006b40, #98e58e);
        }

        .sidebar-card:hover {
            transform: translateY(-6px) scale(1.01);
            border-color: #98e58e;
            box-shadow: 
                0 20px 60px rgba(0, 107, 64, 0.12),
                0 0 30px rgba(152, 229, 142, 0.4),
                inset 0 0 15px rgba(152, 229, 142, 0.05);
            background: linear-gradient(135deg, #ffffff 0%, #f8fbf8 100%);
        }

        .sidebar-title {
            font-size: 1.4em;
            font-weight: 700;
            color: #2d3748;
            margin-bottom: 24px;
            display: flex;
            align-items: center;
            gap: 16px;
        }

        .sidebar-icon {
            width: 48px;
            height: 48px;
            background: linear-gradient(135deg, #006b40, #98e58e);
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 4px;
            box-shadow: 0 4px 12px rgba(0, 107, 64, 0.2);
        }

        .sidebar-icon svg {
            width: 18px;
            height: 18px;
            fill: white;
        }

        .key-achievements {
            list-style: none;
            padding: 0;
        }

        .key-achievements li {
            padding: 16px 0;
            border-bottom: 1px solid rgba(152, 229, 142, 0.1);
            color: #4a5568;
            font-size: 0.95em;
            line-height: 1.6;
        }

        .key-achievements li:last-child {
            border-bottom: none;
        }

        .achievement-highlight {
            color: #006b40;
            font-weight: 700;
        }

        .tech-category {
            margin-bottom: 24px;
            position: relative;
            padding-bottom: 8px;
        }

        /* Dynamic progress bars with awesome effects */
        .tech-category::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            height: 3px;
            background: linear-gradient(90deg, #006b40, #98e58e);
            border-radius: 2px;
            transform: scaleX(0);
            transform-origin: left;
            transition: transform 1s ease-out;
            box-shadow: 0 0 10px rgba(152, 229, 142, 0.5);
        }

        .tech-category.animate::after {
            transform: scaleX(1);
        }

        .tech-category:nth-child(1)::after { width: 95%; }
        .tech-category:nth-child(2)::after { width: 90%; }
        .tech-category:nth-child(3)::after { width: 85%; }
        .tech-category:nth-child(4)::after { width: 92%; }
        .tech-category:nth-child(5)::after { width: 88%; }

        .tech-category h4 {
            font-size: 1.05em;
            font-weight: 700;
            color: #006b40;
            margin-bottom: 10px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .tech-category p {
            font-size: 0.95em;
            line-height: 1.6;
            color: #4a5568;
        }

        .tech-category p strong {
            color: #006b40;
            font-weight: 700;
        }

        .certification-item {
            background: linear-gradient(135deg, #f8fbf8 0%, #ebf9eb 100%);
            border: 1px solid rgba(152, 229, 142, 0.2);
            padding: 20px;
            border-radius: 12px;
            margin-bottom: 16px;
            transition: all 0.3s ease;
        }

        .certification-item:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 24px rgba(0, 107, 64, 0.1);
        }

        .cert-title {
            font-weight: 700;
            color: #006b40;
            margin-bottom: 6px;
            font-size: 1.05em;
        }

        .cert-details {
            color: #4a5568;
            font-size: 0.9em;
        }

        .innovation-spotlight {
            background: linear-gradient(135deg, #006b40 0%, #98e58e 100%);
            color: white;
            padding: 32px;
            border-radius: 16px;
            margin: 24px 0;
            box-shadow: 0 12px 40px rgba(0, 107, 64, 0.2);
            position: relative;
            overflow: hidden;
        }

        .innovation-spotlight::before {
            content: '';
            position: absolute;
            top: -100%;
            left: -100%;
            width: 300%;
            height: 300%;
            background: radial-gradient(circle, rgba(255, 255, 255, 0.1) 0%, transparent 70%);
            animation: spotlightSpin 8s linear infinite;
        }

        @keyframes spotlightSpin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        .innovation-title {
            font-size: 1.3em;
            font-weight: 700;
            margin-bottom: 20px;
            position: relative;
            z-index: 1;
        }

        .innovation-list {
            list-style: none;
            padding: 0;
            position: relative;
            z-index: 1;
        }

        .innovation-list li {
            padding: 8px 0;
            font-size: 0.95em;
            position: relative;
            padding-left: 24px;
            line-height: 1.6;
        }

        .innovation-list li::before {
            content: '→';
            position: absolute;
            left: 0;
            color: rgba(255, 255, 255, 0.8);
            font-weight: bold;
        }

        /* Professional Footer */
        .resume-footer {
            background: linear-gradient(135deg, #2d3748 0%, #4a5568 100%);
            color: white;
            padding: 60px 0 40px;
            margin-top: 60px;
            position: relative;
            overflow: hidden;
        }

        .resume-footer::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: 
                radial-gradient(circle at 25% 25%, rgba(152, 229, 142, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 75% 75%, rgba(0, 107, 64, 0.08) 0%, transparent 40%);
            z-index: 0;
        }

        .footer-content {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
            position: relative;
            z-index: 1;
            text-align: center;
        }

        .footer-statement {
            font-size: 1.8em;
            font-weight: 600;
            margin-bottom: 32px;
            color: #98e58e;
            line-height: 1.4;
        }

        .footer-cta {
            background: linear-gradient(135deg, #006b40, #98e58e);
            color: white;
            padding: 20px 40px;
            border-radius: 50px;
            font-size: 1.2em;
            font-weight: 600;
            text-decoration: none;
            display: inline-block;
            margin: 20px 0 40px;
            transition: all 0.3s ease;
            box-shadow: 0 8px 24px rgba(0, 107, 64, 0.3);
        }

        .footer-cta:hover {
            transform: translateY(-3px);
            box-shadow: 0 12px 32px rgba(0, 107, 64, 0.4);
        }

        .footer-contact {
            display: flex;
            justify-content: center;
            gap: 40px;
            margin-bottom: 40px;
            flex-wrap: wrap;
        }

        .footer-contact-item {
            display: flex;
            align-items: center;
            gap: 12px;
            font-size: 1.1em;
            color: rgba(255, 255, 255, 0.9);
        }

        .footer-copyright {
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            padding-top: 24px;
            color: rgba(255, 255, 255, 0.7);
            font-size: 0.9em;
        }

        /* Responsive */
        @media (max-width: 1024px) {
            .resume-content {
                grid-template-columns: 1fr;
                gap: 32px;
            }

            .header-container {
                grid-template-columns: 1fr;
                text-align: center;
                gap: 32px;
            }

            .metrics-grid {
                grid-template-columns: 1fr;
            }

            .main-content {
                padding: 32px;
            }

            .footer-contact {
                flex-direction: column;
                gap: 20px;
            }
        }

        @media (max-width: 768px) {
            .resume-name {
                font-size: 3em;
            }

            .nav-menu {
                display: none;
            }

            .contact-info {
                flex-direction: column;
                gap: 16px;
                align-items: center;
            }

            .experience-timeline {
                padding-left: 0;
            }

            .experience-timeline::before {
                display: none;
            }

            .experience-card::before {
                display: none;
            }

            .main-content {
                padding: 24px;
            }

            .sidebar-card {
                padding: 24px;
            }

            .footer-statement {
                font-size: 1.4em;
            }

            .footer-contact {
                gap: 16px;
            }
        }

        @media print {
            body {
                background: white;
                color: black;
            }

            .header,
            .resume-footer {
                display: none;
            }

            .resume-content {
                box-shadow: none;
                margin: 0;
                padding: 20px;
            }

            .main-content,
            .sidebar-card {
                box-shadow: none;
                border: 1px solid #ddd;
            }
        }
    </style><style>
body, .logo-text, .nav-menu a {
  font-family: 'Segoe UI', system-ui, -apple-system, sans-serif !important;
}
</style>
<meta charset="utf-8"/>
<meta content="width=device-width, initial-scale=1.0" name="viewport"/>
<title>Contact Kyle Mohney | Product Documentation &amp; Knowledge Operations Leader</title>
<meta content="Get in touch with Kyle Mohney, Senior Product Documentation &amp; Knowledge Operations Leader. Let's collaborate on AI-powered workflows, Sprout Social enterprise strategies, and operational excellence." name="description"/>
<link href="styles.css" rel="stylesheet"/>
<style>
    /* ======== GLOBAL ANIMATIONS ======== */
    @keyframes zoomSlow {
      0% { transform: scale(1); }
      100% { transform: scale(1.05); }
    }
    @keyframes pulseGlow {
      0%, 100% { box-shadow: 0 0 0 rgba(152,229,142,0); }
      50% { box-shadow: 0 0 20px rgba(152,229,142,0.5); }
    }
    @keyframes fadeInUp {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    /* ======== HEADER NAV ======== */
    .header {
      padding: 24px 0;
      position: sticky;
      top: 0;
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(10px);
      z-index: 1000;
      border-bottom: 1px solid rgba(152, 229, 142, 0.1);
      box-shadow: 0 2px 20px rgba(0, 107, 64, 0.05);
    }
    .nav-container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 20px;
      display: flex;
      align-items: center;
      justify-content: space-between;
    }
    .logo {
      display: flex;
      align-items: center;
      gap: 12px;
    }
    .profile-logo {
      width: 52px;
      height: 52px;
      border-radius: 50%;
      border: 2px solid #98e58e;
      object-fit: cover;
    }
    .logo-text {
      font-size: 1.5em;
      font-weight: 700;
      color: #2d3748;
    }
    .nav-menu {
      display: flex;
      list-style: none;
      gap: 40px;
    }
    .nav-menu a {
      color: #2d3748;
      text-decoration: none;
      font-weight: 600;
      font-size: 1.1em;
      padding: 12px 20px;
      border-radius: 8px;
      transition: all 0.3s ease;
    }
    .nav-menu a:hover {
      color: #006b40;
      background: rgba(152, 229, 142, 0.1);
      transform: translateY(-2px);
    }
    .nav-menu a.current {
      color: #006b40;
      background: rgba(152, 229, 142, 0.15);
      box-shadow: 0 4px 12px rgba(152, 229, 142, 0.2);
    }

    /* ======== HERO ======== */
    .contact-hero {
      background: linear-gradient(135deg, #7933ff, #006b40);
      color: white;
      padding: 80px 20px;
    }
    .contact-hero-container {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 40px;
      max-width: 1200px;
      margin: 0 auto;
      align-items: center;
    }
    .hero-text h1 {
      font-size: 3em;
      font-weight: 800;
      margin-bottom: 20px;
    }
    .hero-text p {
      font-size: 1.2em;
      line-height: 1.6;
      max-width: 500px;
      margin-bottom: 30px;
    }
    .hero-cta {
      display: inline-block;
      background: white;
      color: #006b40;
      font-weight: 700;
      padding: 14px 28px;
      border-radius: 8px;
      text-decoration: none;
      transition: all 0.3s ease;
      animation: pulseGlow 2s infinite;
    }
    .hero-cta:hover {
      background: rgba(255,255,255,0.9);
      transform: translateY(-2px);
    }
    .hero-image img {
      max-width: 100%;
      height: auto;
      border-radius: 12px;
      animation: zoomSlow 12s ease-in-out infinite alternate;
    }
    @media (max-width: 900px) {
      .contact-hero-container {
        grid-template-columns: 1fr;
        text-align: center;
      }
      .hero-text p {
        margin-left: auto;
        margin-right: auto;
      }
    }

    /* ======== TRUSTED BY ======== */
    .trusted-strip {
      background: white;
      padding: 20px 0;
      border-bottom: 1px solid rgba(0,0,0,0.05);
    }
    .trusted-container {
      max-width: 1200px;
      margin: 0 auto;
      display: flex;
      gap: 40px;
      overflow-x: auto;
      padding: 0 20px;
      align-items: center;
    }
    .trusted-container img {
      height: 40px;
      flex-shrink: 0;
      filter: grayscale(1) brightness(1.2) contrast(1.1);
      transition: filter 0.3s ease;
    }
    .trusted-container img:hover {
      filter: none;
    }

    /* ======== CONTACT CARDS ======== */
    .contact-methods {
      max-width: 1200px;
      margin: 60px auto;
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 24px;
      padding: 0 20px;
    }
    .contact-card {
      background: white;
      border-radius: 16px;
      padding: 32px;
      box-shadow: 0 8px 24px rgba(0, 107, 64, 0.1);
      text-align: center;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      cursor: pointer;
    }
    .contact-card:hover {
      transform: perspective(800px) rotateY(3deg) rotateX(3deg);
      box-shadow: 0 16px 32px rgba(0, 107, 64, 0.2);
    }
    .contact-card img {
      width: 44px;
      height: 44px;
      margin-bottom: 12px;
      animation: pulseGlow 4s infinite;
    }
    .contact-card h3 {
      margin-top: 0;
      color: #006b40;
      font-size: 1.4em;
      font-weight: 700;
    }
    .contact-card p {
      color: #4a5568;
      font-size: 1em;
      margin-top: 8px;
    }

    /* ======== FORM & MAP ======== */
    .contact-form-section {
      max-width: 1200px;
      margin: 80px auto;
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 40px;
      padding: 0 20px;
      align-items: start;
    }
    .contact-form {
      background: white;
      border-radius: 16px;
      padding: 32px;
      box-shadow: 0 8px 24px rgba(0, 107, 64, 0.1);
      animation: fadeInUp 1s ease forwards;
    }
    .contact-form h2 {
      color: #006b40;
      font-size: 2em;
      margin-bottom: 10px;
    }
    .contact-form small {
      display: block;
      margin-bottom: 20px;
      color: #4a5568;
    }
    .contact-form label {
      display: block;
      margin-top: 16px;
      font-weight: 600;
      color: #2d3748;
    }
    .contact-form input,
    .contact-form textarea {
      width: 100%;
      padding: 12px 14px;
      margin-top: 6px;
      border-radius: 8px;
      border: 1px solid rgba(152, 229, 142, 0.4);
      font-size: 1em;
      transition: all 0.3s ease;
    }
    .contact-form input:focus,
    .contact-form textarea:focus {
      border-color: #006b40;
      box-shadow: 0 0 0 3px rgba(0,107,64,0.15);
      outline: none;
    }
    .contact-form textarea {
      resize: vertical;
      min-height: 120px;
    }
    .contact-form button {
      margin-top: 20px;
      background: linear-gradient(135deg, #006b40, #98e58e);
      color: white;
      padding: 14px 24px;
      font-size: 1em;
      font-weight: 700;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      transition: background 0.3s ease;
    }
    .contact-form button:hover {
      background: linear-gradient(135deg, #005a37, #87d57e);
    }
    .contact-image img {
      width: 100%;
      border-radius: 16px;
      box-shadow: 0 8px 24px rgba(0,0,0,0.15);
    }
    @media (max-width: 768px) {
      .contact-form-section {
        grid-template-columns: 1fr;
      }
    }

    /* ======== FOOTER CTA ======== */
    .footer-cta {
      background: linear-gradient(135deg, #7933ff, #006b40);
      text-align: center;
      padding: 60px 20px;
      color: white;
    }
    .footer-cta h2 {
      font-size: 2em;
      margin-bottom: 20px;
    }
    .footer-cta a {
      background: white;
      color: #006b40;
      padding: 14px 28px;
      border-radius: 8px;
      font-weight: 700;
      text-decoration: none;
      transition: background 0.3s ease;
    }
    .footer-cta a:hover {
      background: rgba(255,255,255,0.9);
    }
  </style>
<style>
.top-contact-btn:hover {
  box-shadow: 0 0 15px rgba(138, 63, 252, 0.8), 0 0 30px rgba(138, 63, 252, 0.5);
  transform: scale(1.05);
  transition: all 0.3s ease;
}
</style>
<style>
.cert-test-section::before {
  content: "";
  position: absolute;
  top: 0; left: 0;
  width: 100%;
  height: 100%;
  background: radial-gradient(circle at calc(85%) 50%, rgba(138,63,252,0.2) 0%, transparent 80%);
  z-index: 0;
  pointer-events: none;
  filter: blur(30px);
}
.cert-test-section {
  position: relative;
  z-index: 1;
}
</style>
<style>
.top-header img {
  max-height: 57px !important;
  height: auto;
}
</style>
<style>
#particles-js-fullpage {
  position: fixed;
  width: 100%;
  height: 100%;
  top: 0;
  left: 0;
  z-index: 0; /* behind everything */
  pointer-events: none;
}
/* Fade-in animation for body */
body {
  animation: fadeIn 1s ease-in-out;
  background-color: #fafafa;
  position: relative;
  z-index: 1;
}
@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}
/* Parallax scrolling effect for node background */
@keyframes subtlePan {
  0% { transform: translateY(0px); }
  50% { transform: translateY(10px); }
  100% { transform: translateY(0px); }
}
#particles-js-fullpage canvas {
  animation: subtlePan 20s ease-in-out infinite;
}
</style><style>
/* Ensure footer Tesla coil stays in footer */
#particles-js-footer {
  position: absolute;
  bottom: 0;
  left: 0;
  width: 100%;
  height: 200px; /* confined to footer height */
  z-index: 0;
  pointer-events: none;
}
footer {
  position: relative;
  overflow: hidden;
}
</style><style>
.cert-card, .testimonial-card {
  transition: all 0.3s ease;
}
</style><style id="resume-header-overrides">
/* === Resume header pixel-perfect overrides === */
.header {
    padding: 24px 0;
    position: sticky;
    top: 0;
    background: rgba(248, 250, 249, 0.95);
    backdrop-filter: blur(10px);
    z-index: 1000;
    border-bottom: 1px solid rgba(152, 229, 142, 0.1);
    box-shadow: 0 2px 20px rgba(0, 107, 64, 0.05);
}
.nav-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 20px;
    display: flex;
    align-items: center;
    justify-content: space-between;
}
.logo { display: flex; align-items: center; gap: 12px; }
.profile-logo { width: 52px; height: 52px; border-radius: 50%; border: 2px solid #98e58e; object-fit: cover; }
.logo-text { font-size: 1.5em; font-weight: 700; color: #2d3748; }
.nav-menu { display: flex; list-style: none; gap: 40px; }
.nav-menu a {
    color: #2d3748;
    text-decoration: none;
    font-weight: 600;
    font-size: 1.1em;
    padding: 12px 20px;
    border-radius: 8px;
    transition: all 0.3s ease;
    position: relative;
}
.nav-menu a:hover { color: #006b40; background: rgba(152, 229, 142, 0.1); transform: translateY(-2px); }
.nav-menu a.current { color: #006b40; background: rgba(152, 229, 142, 0.15); box-shadow: 0 4px 12px rgba(152, 229, 142, 0.2); }
@media (max-width: 768px) { .nav-menu { display: none; } }
</style><style id="tech-background-styles">
/* === Tech background (green lines, grids, nodes) === */
.signal-lines, .tech-connectors, .floating-nodes { position: fixed; top:0; left:0; width:100%; height:100%; pointer-events:none; z-index:0; }
.signal-lines { opacity: .5; }
.signal-line { position:absolute; height:3px; background:linear-gradient(90deg, transparent 0%, rgba(152,229,142,.8) 20%, #98e58e 50%, rgba(152,229,142,.8) 80%, transparent 100%); border-radius:2px; animation: signalFlow 4s linear infinite; box-shadow:0 0 10px rgba(152,229,142,.5); }
.signal-line:nth-child(1){ top:20%; left:0; width:100%; animation-delay:0s; }
.signal-line:nth-child(2){ top:50%; left:0; width:100%; animation-delay:1.5s; }
.signal-line:nth-child(3){ top:75%; left:0; width:100%; animation-delay:3s; }
.signal-vertical{ position:absolute; width:3px; background:linear-gradient(0deg, transparent 0%, rgba(152,229,142,.6) 20%, #98e58e 50%, rgba(152,229,142,.6) 80%, transparent 100%); border-radius:2px; animation: signalFlowVertical 5s linear infinite; box-shadow:0 0 8px rgba(152,229,142,.4); }
.signal-vertical:nth-child(4){ left:25%; top:0; height:100%; animation-delay:.5s; }
.signal-vertical:nth-child(5){ left:60%; top:0; height:100%; animation-delay:2s; }
.signal-vertical:nth-child(6){ left:85%; top:0; height:100%; animation-delay:3.5s; }
@keyframes signalFlow{ 0%{ transform: translateX(-100%) scaleX(0); opacity:0; } 20%{ transform: translateX(-50%) scaleX(1); opacity:1; } 80%{ transform: translateX(50%) scaleX(1); opacity:1; } 100%{ transform: translateX(100%) scaleX(0); opacity:0; } }
@keyframes signalFlowVertical{ 0%{ transform: translateY(-100%) scaleY(0); opacity:0; } 20%{ transform: translateY(-50%) scaleY(1); opacity:1; } 80%{ transform: translateY(50%) scaleY(1); opacity:1; } 100%{ transform: translateY(100%) scaleY(0); opacity:0; } }
.tech-connectors{ opacity:.25; }
.circuit-connector{ position:absolute; background:linear-gradient(90deg, transparent, rgba(152,229,142,.6), transparent); height:2px; animation: pulseData 4s ease-in-out infinite; }
.circuit-connector:nth-child(1){ top:25%; left:0; width:100%; animation-delay:0s; }
.circuit-connector:nth-child(2){ top:50%; left:0; width:100%; animation-delay:1s; }
.circuit-connector:nth-child(3){ top:75%; left:0; width:100%; animation-delay:2s; }
.circuit-connector.vertical{ width:2px; height:100%; background:linear-gradient(0deg, transparent, rgba(152,229,142,.6), transparent); }
.circuit-connector.vertical:nth-child(4){ left:20%; top:0; animation-delay:.5s; }
.circuit-connector.vertical:nth-child(5){ left:50%; top:0; animation-delay:1.5s; }
.circuit-connector.vertical:nth-child(6){ left:80%; top:0; animation-delay:2.5s; }
@keyframes pulseData{ 0%,100%{ opacity:.2; transform:scaleX(1);} 50%{ opacity:.8; transform:scaleX(1.05);} }
.floating-nodes{ z-index:0; }
.data-node{ position:absolute; width:6px; height:6px; background: radial-gradient(circle, rgba(152,229,142,.9) 0%, rgba(0,107,64,.6) 100%); border-radius:50%; animation: nodeFloat 15s ease-in-out infinite; box-shadow:0 0 20px rgba(152,229,142,.4), 0 0 40px rgba(152,229,142,.2); opacity:.8; }
.data-node:nth-child(1){ top:20%; left:10%; animation-delay:0s; }
.data-node:nth-child(2){ top:60%; left:85%; animation-delay:2s; }
.data-node:nth-child(3){ top:80%; left:25%; animation-delay:4s; }
.data-node:nth-child(4){ top:30%; left:70%; animation-delay:6s; }
.data-node:nth-child(5){ top:15%; left:45%; animation-delay:8s; }
.data-node:nth-child(6){ top:75%; left:60%; animation-delay:10s; }
.data-node:nth-child(7){ top:40%; left:15%; animation-delay:12s; }
.data-node:nth-child(8){ top:65%; left:90%; animation-delay:14s; }
.data-node:nth-child(9){ top:5%; left:75%; animation-delay:1s; }
.data-node:nth-child(10){ top:90%; left:35%; animation-delay:3s; }
.data-node:nth-child(11){ top:45%; left:95%; animation-delay:5s; }
.data-node:nth-child(12){ top:85%; left:5%; animation-delay:7s; }
@keyframes nodeFloat{ 0%,100%{ transform:translate3d(0,0,0) scale(1); opacity:.6; box-shadow:0 0 20px rgba(152,229,142,.4);} 25%{ transform:translate3d(30px,-40px,0) scale(1.3); opacity:1; box-shadow:0 0 30px rgba(152,229,142,.6);} 50%{ transform:translate3d(-20px,30px,0) scale(.8); opacity:.8; box-shadow:0 0 25px rgba(152,229,142,.5);} 75%{ transform:translate3d(40px,15px,0) scale(1.2); opacity:.9; box-shadow:0 0 35px rgba(152,229,142,.7);} }
/* ensure content sits over background */
body { position: relative; z-index: 1; }
</style><style>
/* === Normalize testimonial card layout === */
.testimonial-test-card {
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  height: 100%;
}
.testimonial-test-card p {
  margin-bottom: 16px;
  line-height: 1.5;
}
.testimonial-test-card .author {
  margin: 0;
  font-weight: 700;
  line-height: 1.2;
}
.testimonial-test-card .role {
  margin: 0;
  font-weight: 500;
  font-size: 0.95rem;
  opacity: 0.8;
}
</style></head>
<body><div class="signal-lines"><div class="signal-line"></div><div class="signal-line"></div><div class="signal-line"></div><div class="signal-vertical"></div><div class="signal-vertical"></div><div class="signal-vertical"></div></div><div class="tech-connectors"><div class="circuit-connector"></div><div class="circuit-connector"></div><div class="circuit-connector"></div><div class="circuit-connector vertical"></div><div class="circuit-connector vertical"></div><div class="circuit-connector vertical"></div></div><div class="floating-nodes"><div class="data-node"></div><div class="data-node"></div><div class="data-node"></div><div class="data-node"></div><div class="data-node"></div><div class="data-node"></div><div class="data-node"></div><div class="data-node"></div><div class="data-node"></div><div class="data-node"></div><div class="data-node"></div><div class="data-node"></div></div>
<!-- Full Page Particle Overlay -->
<div id="particles-js-fullpage"></div>
<style>
#particles-js-fullpage {
  position: fixed;
  width: 100%;
  height: 100%;
  top: 0;
  left: 0;
  z-index: 0;
  pointer-events: none;
}
body {
  position: relative;
  z-index: 1;
}
</style>
<!-- Particle Background -->
<style>
#particles-js {
  position: absolute;
  width: 100%;
  height: 100%;
  top: 0;
  left: 0;
  z-index: 0;
}
</style>
<!-- Header Navigation -->
<header class="header">
<div class="nav-container">
<div class="logo">
<img alt="Kyle Mohney" class="profile-logo" src="assets/Kyle.png"/>
<span class="logo-text">Kyle Mohney</span>
</div>
<nav>
<ul class="nav-menu">
<li><a href="index.html">Home</a></li>
<li><a href="resume.html">Resume</a></li>
<li><a href="projects.html">Projects</a></li>
<li><a href="articles.html">Articles</a></li>
<li><a href="about.html">About</a></li>
<li><a class="current" href="contact.html">Contact</a></li>
</ul>
</nav>
</div>
</header>
<!-- Hero -->
<section class="contact-hero" style="position: relative; overflow: hidden;"><div id="particles-js-hero" style="position:absolute; inset:0; z-index:0; pointer-events:none;"></div>
<div class="contact-hero-container" style="position: relative; z-index: 1;">
<div class="hero-text">
<h1 class="glow-text">Let’s Build Something Worth Talking About</h1>
<p>Transforming knowledge operations and AI workflows into scalable, ROI-driving systems that set the gold standard for enterprise performance.</p>
<a class="hero-cta" href="#contact-form"> Let’s Talk</a>
</div>
<div class="hero-image" style="position: relative; z-index: 1;">
<img alt="Futuristic Technology Representation" src="assets/contact_top.png"/>
</div>
</div>
</section>
<!-- ===== MODERN CERTIFICATIONS + TESTIMONIALS SECTION ===== -->
<style>
/* Section Background */
.cert-test-section {
  padding: 80px 20px;
  background: white;
  position: relative;
  overflow: hidden;
  color: #006b40;
}

.cert-particle-bg {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: radial-gradient(circle at 10% 20%, rgba(255,255,255,0.05) 0%, transparent 50%),
              radial-gradient(circle at 80% 30%, rgba(255,255,255,0.05) 0%, transparent 50%),
              radial-gradient(circle at 50% 80%, rgba(255,255,255,0.05) 0%, transparent 50%);
  animation: certParticleMove 15s infinite alternate ease-in-out;
  z-index: 0;
}

@keyframes certParticleMove {
  0% { background-position: 10% 20%, 80% 30%, 50% 80%; }
  100% { background-position: 15% 25%, 75% 35%, 55% 75%; }
}

/* Section Headings */
.cert-test-section h2 {
  text-align: center;
  font-size: 2.5rem;
  font-weight: 800;
  margin-bottom: 10px;
  position: relative;
  z-index: 2;
}

.cert-test-section p.section-sub {
  text-align: center;
  font-size: 1.1rem;
  color: rgba(255,255,255,0.85);
  margin-bottom: 50px;
  position: relative;
  z-index: 2;
}

/* Glassmorphism Card Base */
.glass-card {
  background: rgba(255,255,255,0.08);
  backdrop-filter: blur(8px);
  border-radius: 16px;
  padding: 20px;
  box-shadow: 0 8px 24px rgba(0,0,0,0.2);
  transition: transform 0.3s ease, box-shadow 0.3s ease;
  opacity: 0;
  transform: translateY(20px);
}

.glass-card.visible {
  opacity: 1;
  transform: translateY(0);
  transition: opacity 0.6s ease, transform 0.6s ease;
}

.glass-card:hover {
  transform: translateY(-6px);
  box-shadow: 0 12px 32px rgba(0,0,0,0.3);
}

/* Certifications Grid */
.cert-test-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 25px;
  margin-bottom: 70px;
  position: relative;
  z-index: 2;
}

.cert-test-card {
  text-align: center;
  font-weight: bold;
  font-size: 1rem;
  color: white;
}

.cert-test-card a {
  text-decoration: none;
  color: #006b40;
}

.cert-icon {
  width: 40px;
  height: 10px;
  margin-bottom: 12px;
}

/* Testimonials Carousel */
.testimonial-carousel {
  width: 100%;
  max-width: 800px;
  margin: 0 auto;
  position: relative;
}

.carousel-container {
  position: relative;
  overflow: hidden;
}

.carousel-track {
  display: flex;
  transition: transform 0.5s ease;
}

.carousel-slide {
  flex: 0 0 100%;
  width: 100%;
}

.carousel-button {
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  background: rgba(255, 255, 255, 0.8);
  border: none;
  width: 40px;
  height: 40px;
  border-radius: 50%;
  cursor: pointer;
  z-index: 3;
}

.carousel-button.prev {
  left: -20px;
}

.carousel-button.next {
  right: -20px;
}

.carousel-dots {
  display: flex;
  justify-content: center;
  margin-top: 20px;
}

.carousel-dot {
  width: 10px;
  height: 10px;
  background: rgba(0, 0, 0, 0.3);
  border-radius: 50%;
  margin: 0 5px;
  cursor: pointer;
}

.carousel-dot.active {
  background: #006b40;
}

.testimonial-test-card {
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  padding: 20px;
}

.testimonial-test-card p {
  font-style: italic;
  font-size: 1rem;
  line-height: 1.5;
  margin-bottom: 15px;
}

.testimonial-test-card .author {
  font-weight: bold;
}

.testimonial-test-card .role {
  font-size: 0.9rem;
  opacity: 0.85;
}
</style>
<div class="cert-test-section">
<div class="cert-particle-bg"></div>
<h2>Trusted By Industry Leaders</h2>
<p class="section-sub glow-text" style="color: #006b40;">Proven expertise backed by globally recognized certifications</p>
<div class="cert-test-grid">
<div class="glass-card cert-test-card" style="background-color: #ffffff; border: 1px solid #e0e0e0; box-shadow: 0 8px 24px rgba(0,0,0,0.1);">
<a href="https://app-na2.hubspot.com/academy/achievements/nzgwvnl5/en/1/kyle-mohney/inbound" target="_blank">HubSpot Inbound</a>
</div>
<div class="glass-card cert-test-card" style="background-color: #ffffff; border: 1px solid #e0e0e0; box-shadow: 0 8px 24px rgba(0,0,0,0.1);">
<a href="https://www.linkedin.com/in/kyle-m-ab0822214/details/certifications/1752528514194" target="_blank">HubSpot Revenue Operations</a>
</div>
<div class="glass-card cert-test-card" style="background-color: #ffffff; border: 1px solid #e0e0e0; box-shadow: 0 8px 24px rgba(0,0,0,0.1);">
<a href="https://www.linkedin.com/in/kyle-m-ab0822214/details/certifications/1752596011573" target="_blank">HubSpot SEO 1</a>
</div>
<div class="glass-card cert-test-card" style="background-color: #ffffff; border: 1px solid #e0e0e0; box-shadow: 0 8px 24px rgba(0,0,0,0.1);">
<a href="https://www.linkedin.com/in/kyle-m-ab0822214/details/certifications/1752673536813" target="_blank">HubSpot SEO 2</a>
</div>
<div class="glass-card cert-test-card" style="background-color: #ffffff; border: 1px solid #e0e0e0; box-shadow: 0 8px 24px rgba(0,0,0,0.1);">
<a href="https://www.linkedin.com/in/kyle-m-ab0822214/details/certifications/1752173505380" target="_blank">ServiceHub Software</a>
</div>
<div class="glass-card cert-test-card" style="background-color: #ffffff; border: 1px solid #e0e0e0; box-shadow: 0 8px 24px rgba(0,0,0,0.1);">
<a href="https://www.linkedin.com/in/kyle-m-ab0822214/details/certifications/1752773976566" target="_blank">Google: Generative AI</a>
</div>
</div>
<h2>What My Peers Say</h2>
<p class="section-sub glow-text" style="color: #006b40;">Real words from colleagues who know my work best</p>
<div class="testimonial-test-grid">
<div class="glass-card testimonial-test-card" style="display: flex; flex-direction: column; justify-content: space-between; height: 220px; padding: 12px 20px; text-align: left;">
<p style="margin: 0; padding: 0; line-height: 1.2;">“Keep pushing and experimenting with new tech, your journey is real inspiring.”</p>
<div class="author" style="margin: 0; padding: 0; line-height: 1.2;">Oleg Zankov</div>
<div class="role" style="margin: 0; padding: 0; line-height: 1.2;">Co-Founder &amp; Product Owner | Latenode.com | Debexpert.com</div>
</div>
<div class="glass-card testimonial-test-card" style="display: flex; flex-direction: column; justify-content: space-between; height: 220px; padding: 12px 20px; text-align: left;">
<p style="margin: 0; padding: 0; line-height: 1.2;">"Kyle is one of the most dedicated and passionate people I’ve worked with... helping shape better processes and policies that benefit both our teams and our end users at Thumbtack."</p>
<div class="author" style="margin: 0; padding: 0; line-height: 1.2;">Ma. Christina Reyes-Caacoy</div>
<div class="role" style="margin: 0; padding: 0; line-height: 1.2;">People Leader | Service Operations | Thumbtack (Former)</div>
</div>
<div class="glass-card testimonial-test-card" style="display: flex; flex-direction: column; justify-content: space-between; height: 220px; padding: 12px 20px; text-align: left;">
<p style="margin: 0; padding: 0; line-height: 1.2;">"Kyle is one of the smartest and most passionate people I’ve worked with when it comes to Customer Experience. Any team would be lucky to have his dedication and insight."</p>
<div class="author" style="margin: 0; padding: 0; line-height: 1.2;">Oliver Rempillo II</div>
<div class="role" style="margin: 0; padding: 0; line-height: 1.2;">Head of Service Operations | Thumbtack (Former)</div>
</div>
</div>
</div>
<script>
document.addEventListener('DOMContentLoaded', () => {
    const track = document.querySelector('.carousel-track');
    const slides = Array.from(track.children);
    const nextButton = document.querySelector('.carousel-button.next');
    const prevButton = document.querySelector('.carousel-button.prev');
    const dotsContainer = document.querySelector('.carousel-dots');

    // Create dot indicators
    slides.forEach((_, index) => {
        const dot = document.createElement('button');
        dot.classList.add('carousel-dot');
        if (index === 0) dot.classList.add('active');
        dotsContainer.appendChild(dot);
    });

    const dots = Array.from(document.querySelectorAll('.carousel-dot'));

    // Set up initial slide positions
    const slideWidth = slides[0].getBoundingClientRect().width;
    slides.forEach((slide, index) => {
        slide.style.left = slideWidth * index + 'px';
    });

    let currentSlide = 0;

    // Update slide position
    const moveToSlide = (targetIndex) => {
        track.style.transform = `translateX(-${slideWidth * targetIndex}px)`;
        
        // Update active dot
        dots.forEach(dot => dot.classList.remove('active'));
        dots[targetIndex].classList.add('active');
        
        currentSlide = targetIndex;
    };

    // Next button click
    nextButton.addEventListener('click', () => {
        const nextIndex = (currentSlide + 1) % slides.length;
        moveToSlide(nextIndex);
    });

    // Previous button click
    prevButton.addEventListener('click', () => {
        const prevIndex = (currentSlide - 1 + slides.length) % slides.length;
        moveToSlide(prevIndex);
    });

    // Dot click
    dots.forEach((dot, index) => {
        dot.addEventListener('click', () => {
            moveToSlide(index);
        });
    });

    // Auto-advance every 5 seconds
    setInterval(() => {
        const nextIndex = (currentSlide + 1) % slides.length;
        moveToSlide(nextIndex);
    }, 5000);
});
</script>
<!-- ===== END MODERN CERTIFICATIONS + TESTIMONIALS ===== -->
<!-- Contact Methods -->
<div class="contact-methods">
<a class="contact-card" href="tel:+17208777466" style="background-color: rgba(255,255,255,0.85);">
<img alt="Phone Icon" src="assets/phone.png"/>
<h3>Direct Line</h3>
<p>Immediate Collaboration</p>
</a>
<a class="contact-card" href="mailto:Kyle.Mohney@hotmail.com" style="background-color: rgba(255,255,255,0.85);">
<img alt="Email Icon" src="assets/mail.png"/>
<h3>Email Me</h3>
<p>Skip the back-and-forth — let’s talk solutions</p>
</a>
<a class="contact-card" href="https://www.linkedin.com/in/kyle-mohney" style="background-color: rgba(255,255,255,0.85);" target="_blank">
<img alt="LinkedIn Icon" src="assets/in.png"/>
<h3>LinkedIn</h3>
<p>Where strategy meets execution</p>
</a>
</div>
<!-- Contact Form + Map -->
<div class="contact-form-section" id="contact-form">
<div class="contact-form">
<h2>Send a Message</h2>
<small>I respond personally within 24 hours</small>
<form id="contactForm">
<label for="name">Name</label>
<input id="name" name="name" required="" type="text"/>
<label for="email">Email</label>
<input id="email" name="email" required="" type="email"/>
<label for="company">Company</label>
<input id="company" name="company" type="text"/>
<label for="message">Message</label>
<textarea id="message" name="message" required=""></textarea>
<button type="submit">SEND IT</button>
</form>
</div>
<div class="contact-image">
<img alt="Map of Niles, Ohio" src="assets/map.png"/>
</div>
</div>
<script>
    document.getElementById('contactForm').addEventListener('submit', function(e) {
      e.preventDefault();
      const name = document.getElementById('name').value.trim();
      const email = document.getElementById('email').value.trim();
      const company = document.getElementById('company').value.trim();
      const message = document.getElementById('message').value.trim();

      if (!name || !email || !message) {
        alert("Please fill in all required fields.");
        return;
      }

      const subject = encodeURIComponent(`Opportunity with Sprout Social – ${name}${company ? " at " + company : ""}`);
      const body = encodeURIComponent(message + `\n\n---\nReply to: ${email}`);
      
      window.location.href = `mailto:Kyle.Mohney@hotmail.com?subject=${subject}&body=${body}`;
    });
  </script>
<!-- Load Particle Library -->
<!-- Footer Particle Background -->
<div id="particles-js-footer"></div>
<style>
#particles-js-footer {
  position: absolute;
  width: 100%;
  height: 200px;
  bottom: 0;
  left: 0;
  z-index: 0;
}
.footer-container {
  position: relative;
  z-index: 1;
}
</style>
<footer style="width: 100%; padding: 40px 0; text-align: center; background: linear-gradient(90deg, #003b2f, #001c44); color: white; font-style: italic;">"Well done is better than well said." – Benjamin Franklin</footer><script>
document.addEventListener("DOMContentLoaded", function() {
  const track = document.querySelector(".carousel-track");
  const prevBtn = document.getElementById("prev");
  const nextBtn = document.getElementById("next");
  let position = 0;

  nextBtn?.addEventListener("click", () => {
    position -= 100;
    if (position < -200) position = 0;
    track.style.transform = `translateX(${position}%)`;
  });

  prevBtn?.addEventListener("click", () => {
    position += 100;
    if (position > 0) position = -200;
    track.style.transform = `translateX(${position}%)`;
  });
});
</script></body>
</html>
